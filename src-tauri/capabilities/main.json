{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "main",
  "description": "The settings window is moved by its own toolbar",
  "windows": ["main"],
  "permissions": ["core:window:allow-start-dragging"]
}
